info:
  name: PATCH Workout
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8000/api/v1/workouts/1
  body:
    type: json
    data: |-
      {
        "title": "Mayor flexibilidad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
