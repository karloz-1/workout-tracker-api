info:
  name: PATCH Progress
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8000/api/v1/progress/1
  body:
    type: json
    data: |-
      {
        "weight": "90"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
