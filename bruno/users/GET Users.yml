info:
  name: GET Users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/users/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
